program bstm_1822_maiwanju_2017_2_6;
var
  a,b,t,c:array[1..100000] of longint;
  d:array[1..100000] of boolean;
  n,m,k,s,i,j,mid:longint;
function p(n,k,s:longint):boolean;
var i:longint;
  function dfs(n1,kk,k1,ok,s:longint):boolean;
  begin
    if s<0 then exit(false);
    if ok=k1 then exit(true);
    if kk=m+1 then exit;
    if not
begin
  if n=1 then exit(dfs(1,1,k,0,s));

begin
  readln(n,m,k,s);
  for i:=1 to n do read(a[i]);
  for i:=1 to n do read(b[i]);
  for i:=1 to m do readln(t[i],c[i]);
  i:=1; j:=n;
  while i<=j do
  begin
    mid:=(i+j)div 2;
    if


