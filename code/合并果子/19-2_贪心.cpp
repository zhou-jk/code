#include <cstdio>

const int N = 10000;
int a[N], b[N];

int main()
{
	int n, lA = 0, rA, lB = 0, rB = 0;
	scanf("%d", &n);
	rB = n;
	for (int i = 0; i < n; ++i) scanf("%d", a + i);
    while (--n)
    {
    }
	return 0;
}